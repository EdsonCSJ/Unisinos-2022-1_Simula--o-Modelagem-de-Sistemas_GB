import src.Entity;
import src.EntitySet;

public class test {
    public static void main(String[] args) {

        ArrivalEvent arrivalE = new ArrivalEvent("chegada", "FIFO", 10);
        OrderingEvent ordE = new OrderingEvent("filaCaixas", duration, clerks)
        for (int i = 0; i < 10; i++) {
            arrivalE.clientsArrival(new Clients("name", i), i);

        }


    }
}
